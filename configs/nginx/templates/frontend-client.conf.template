# Frontend client server
server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name ${FRONTEND_CLIENT_SERVER_NAME};
    location / {
        proxy_pass http://frontend-client:3000;
    }
}
